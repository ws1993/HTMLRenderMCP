# HTML Render MCP 技术路线与 Cherry Studio 流式渲染能力解析

## 1. 结论概览

当前项目是一个面向 Cherry Studio 的本地 MCP Server，核心目标是把结构化页面数据稳定渲染成可直接显示在 Cherry Studio 对话消息里的 HTML 片段。

整体技术路线可以概括为：

```txt
本地 MCP stdio server
  -> 三个 one-shot HTML render tools
  -> Zod 结构化输入校验
  -> basic / upgraded / adaptive 三套渲染器
  -> inline-styled HTML fragment
  -> Cherry Studio 消息区 raw HTML 渲染
```

需要特别区分的是：当前项目本身并没有在 MCP 工具层实现 HTML 结果流式推送。三个工具都是一次性接收完整结构化输入、一次性生成完整 HTML 字符串、一次性返回 MCP text content。

接入 Cherry Studio 后看到的“HTML 流式渲染”效果，主要来自 Cherry Studio 自身的消息流处理能力：

```txt
AI / Provider stream
  -> TEXT_DELTA / MCP_TOOL_* 等 chunk
  -> StreamProcessingService 分发
  -> message block 状态更新
  -> React UI 重新渲染
  -> ReactMarkdown + rehypeRaw 解析 raw HTML
  -> 形成 HTML 逐步显示效果
```

也就是说，当前项目提供的是最终 HTML 生成能力；Cherry Studio 提供的是消息流式显示和 raw HTML 渲染能力。

---

## 2. 当前项目的定位

项目名称为 `html-render-mcp`，定位是：

> 面向 Cherry Studio 的本地 MCP Server，用于把完整结构化页面一次性渲染成可直接显示在对话消息里的 HTML 片段。

从 `package.json` 可以看出，项目采用轻量 TypeScript + Node.js 技术栈：

- Node.js `>=20`
- TypeScript ESM
- `@modelcontextprotocol/sdk`：MCP Server 与 stdio transport
- `zod`：运行时输入校验
- `esbuild`：构建打包
- `tsx`：开发时直接运行 TypeScript

常用脚本：

```txt
npm run dev    -> tsx src/index.ts
npm run build  -> tsc --noEmit && node scripts/build.mjs
npm start      -> node dist/index.js
npm run check  -> tsc --noEmit
npm test       -> node --test --import tsx src/test/**/*.test.ts
```

在 Cherry Studio 中的接入方式是标准 MCP 本地进程配置：

```json
{
  "mcpServers": {
    "html-render-mcp": {
      "command": "node",
      "args": ["E:/Project/AI/HTMLRenderMCP/dist/index.js"],
      "cwd": "E:/Project/AI/HTMLRenderMCP"
    }
  }
}
```

---

## 3. 项目总体架构

项目按 MCP server、工具、schema、renderer、style 分层组织。主链路如下：

```txt
src/index.ts
  -> src/server/createMcpServer.ts
    -> src/server/toolRegistry.ts
      -> src/tools/*
        -> src/renderers/*
          -> src/renderers/{shared,upgraded,adaptive}/*
          -> src/styles/{basic,upgraded,adaptive}/index.ts
```

### 3.1 入口层

`src/index.ts` 只负责创建 MCP Server 并连接 stdio transport。它不包含工具定义、schema、渲染逻辑或样式逻辑。

### 3.2 MCP Server 层

`src/server/createMcpServer.ts` 负责：

- 创建 `Server`
- 声明 MCP capability：`tools`
- 处理 `ListToolsRequestSchema`
- 处理 `CallToolRequestSchema`
- 根据工具名分发到对应 tool handler
- 将异常统一转成 MCP tool error content

当前 server 通过 stdio 和 Cherry Studio 通信，没有 HTTP API、WebSocket 或主动推送通道。

### 3.3 工具注册层

`src/server/toolRegistry.ts` 当前只注册三个工具：

- `render_final_html`
- `render_upgraded_html`
- `render_adaptive_theme_html`

注册表中的禁用工具提示也明确强调：当前 server 只暴露这三个面向 Cherry Studio 的 one-shot HTML output 工具。

### 3.4 工具 handler 层

每个工具一个 handler，位于 `src/tools/*`：

- `src/tools/renderFinalHtmlTool.ts`
- `src/tools/renderUpgradedHtmlTool.ts`
- `src/tools/renderAdaptiveThemeHtmlTool.ts`

工具 handler 的职责是：

1. 标明工具名、描述、MCP input schema。
2. 对输入做兼容性归一化。
3. 通过 Zod schema 校验输入。
4. 调用对应 renderer。
5. 将 HTML 字符串包装成 MCP text content 返回。

---

## 4. 三条 HTML 渲染路线

当前项目有三条主要渲染路线，分别对应三个 MCP 工具。

---

### 4.1 基础渲染：`render_final_html`

基础渲染路线适合生成简单落地页、报告、文章和仪表盘类页面。

数据流：

```txt
render_final_html
  -> normalizeRenderFinalHtmlArguments
  -> htmlPageSchema
  -> renderInlineHtmlFragment
  -> textContent(html)
```

支持的模板包括：

- `landing-page`
- `report`
- `article`
- `dashboard`

支持的基础区块包括：

- `hero`
- `features`
- `content`
- `steps`
- `faq`

这条路线可以理解为“固定模板 + 固定 section + inline style”的基础能力。

---

### 4.2 升级渲染：`render_upgraded_html`

升级渲染路线适合研究、对比、教程、解读、清单、观点等更结构化的内容。

数据流：

```txt
render_upgraded_html
  -> normalizeRenderUpgradedHtmlArguments
  -> upgradedHtmlPageSchema
  -> renderUpgradedInlineHtmlFragment
  -> renderBlock(...)
  -> renderFooter(...)
  -> textContent(html)
```

它的核心抽象是：

```txt
内容类型 contentTypes
  + 设计令牌 tokens
  + 布局组件 blocks
  -> 稳定 HTML fragment
```

支持的内容类型：

- `news`
- `research`
- `explain`
- `compare`
- `tutorial`
- `list`
- `opinion`

支持的布局组件：

- `hero`
- `summary-card`
- `timeline`
- `stat-grid`
- `comparison-table`
- `quote`
- `risk-box`
- `faq`
- `steps`
- `source-list`
- `callout`

支持的设计令牌包括主色、强调色、字号比例、圆角、间距密度、边框风格、阴影层级等。

这条路线的优势是：不让模型自由拼接复杂 HTML，而是让模型选择有限的内容类型和布局组件，由 MCP 统一生成稳定、安全、风格一致的 HTML。

---

### 4.3 自适应主题渲染：`render_adaptive_theme_html`

自适应主题渲染是当前项目最有价值、也最适合继续演进的路线。

数据流：

```txt
render_adaptive_theme_html
  -> normalizeRenderAdaptiveThemeHtmlArguments
  -> adaptiveThemeHtmlPageSchema
  -> renderAdaptiveThemeInlineHtmlFragment
      -> resolveAdaptiveContext
          -> resolveAdaptiveStyleProfile
          -> resolveAdaptiveProfileDefinition
          -> resolveAdaptiveExpressionStrategy
      -> getResolvedExpressions
      -> renderAdaptiveExpression(...)
      -> renderAdaptiveBlock(...)
      -> renderFooter(...)
  -> textContent(html)
```

这条路线不是简单按主题换皮，而是一个主题感知的信息表达系统。它会根据 `contentTypes`、`styleProfile`、`expression.strategy` 等信息，自动选择更合适的视觉画像和信息结构。

典型映射关系：

| 内容类型 | 自动画像 | 默认表达策略 | 适合场景 |
|---|---|---|---|
| `news` | `old-newspaper` | `inverted-pyramid` | 新闻、事件追踪、每日要闻 |
| `research` | `academic-journal` | `academic` | 调研、研究、证据型分析 |
| `compare` | `decision-brief` | `decision` | 选型、对比、决策建议 |
| `tutorial` | `workshop-guide` | `workshop` | 教程、操作说明、工作坊路径 |
| `list` | `curated-list` | `catalog` | 排名、推荐、精选清单 |
| `opinion` | `editorial-column` | `argument` | 评论、观点文章、论证 |
| `explain` | `clean-magazine` | `top-down` | 科普、解释、综述 |

可以概括为：

```txt
内容类型
  -> 视觉画像
    -> 表达策略
      -> 语义表达块 expressions
        -> 稳定 HTML fragment
```

支持的语义表达包括：

- `lead`：导语或核心观点
- `key-takeaways`：要点速览
- `executive-summary`：执行摘要或决策简报
- `evidence-map`：论点、证据、置信度和限制
- `decision-matrix`：标准、选项、评分和推荐
- `argument-map`：主张、理由、反方观点和结论
- `process-guide`：目标、前置条件、步骤、检查点和验收
- `ranked-list`：排名、标签、适配场景和说明
- `section-outline`：层级化大纲

相比模板库，自适应路线更适合长期演进。后续只需要扩展 profile、expression、block 和 token，而不是维护大量固定模板。

---

## 5. Cherry Studio 适配策略

Cherry Studio 的消息区不是完整浏览器页面容器，而是一个 Markdown/HTML 渲染环境。因此当前项目做了几项关键适配。

### 5.1 输出 HTML fragment

项目输出的是片段级 HTML，例如：

```html
<div data-html-render-mcp="adaptive-theme-inline" style="...">
  ...
</div>
```

而不是完整页面：

```html
<!doctype html>
<html>
  <head>...</head>
  <body>...</body>
</html>
```

这可以避免 Cherry Studio 消息区无法处理完整 document 结构的问题。

### 5.2 全部使用行内样式

聊天消息环境通常会限制或清理 `<style>`、外部 CSS、`script` 等内容。因此项目将样式全部写成 inline style，降低被消息渲染环境屏蔽的概率。

### 5.3 默认转义用户文本

项目通过共享工具处理 HTML 安全：

- `escapeHtml`
- `escapeAttribute`
- `normalizeRenderableHref`
- `renderInlineRichText`

链接协议白名单只允许：

- `http:`
- `https:`
- `mailto:`
- `tel:`

正文长文本只放行少量安全内联标签，例如：

- `<strong>` / `<b>`
- `<em>` / `<i>`
- `<u>`
- `<s>`
- `<code>`
- `<mark>`
- `<small>`
- `<sub>` / `<sup>`
- `<br>`

其他标签和带属性的标签会被转义。这一点非常重要，因为 Cherry Studio 支持 raw HTML 渲染，如果 MCP 不做安全边界，容易引入 XSS 风险。

---

## 6. 当前项目与“流式渲染”的关系

严格来说，当前三个 MCP 工具没有实现 MCP 结果层面的流式 HTML 输出。

以 `render_adaptive_theme_html` 为例，工具执行逻辑是：

```txt
接收完整 page JSON
  -> Zod 校验
  -> renderAdaptiveThemeInlineHtmlFragment(page)
  -> 得到完整 HTML 字符串
  -> textContent(html)
  -> 一次性返回
```

因此，MCP 调用结果是一次性返回的，而不是服务端持续推送片段。

项目中虽然存在本地预览相关的 SSE 基础设施：

```txt
src/live/previewServer.ts
src/live/liveStore.ts
```

其中 `previewServer.ts` 使用了：

```txt
Content-Type: text/event-stream
EventSource(...)
event: refresh
```

这套能力用于本地 live preview 页面刷新，不在当前 `toolRegistry.ts` 注册的 MCP 工具中。因此它不是当前 Cherry Studio one-shot 输出链路的一部分。

---

## 7. Cherry Studio 的底层流式能力

根据 Cherry Studio 公开源码和文档，可以将其底层能力分成五层理解。

### 7.1 MCP 工具接入层

Cherry Studio 支持通过配置启动 MCP Server，本项目就是被 Cherry 作为本地子进程启动，然后通过 MCP stdio 协议完成：

```txt
listTools
callTool
receive tool result
```

本项目对 Cherry 来说就是一个标准 MCP 工具提供者。

### 7.2 Chunk-based streaming 系统

Cherry Studio 内部定义了多种 chunk 类型，包括：

```txt
TEXT_START
TEXT_DELTA
TEXT_COMPLETE

MCP_TOOL_CREATED
MCP_TOOL_PENDING
MCP_TOOL_IN_PROGRESS
MCP_TOOL_COMPLETE
MCP_TOOL_STREAMING

IMAGE_CREATED
IMAGE_DELTA
IMAGE_COMPLETE
```

这说明 Cherry 的消息流不是一个简单字符串，而是多类型事件流，包括文本、工具、图片、搜索、推理、错误等。

其中 `MCP_TOOL_STREAMING` 更偏向工具参数流式生成和工具状态更新，不等价于 MCP Server 对 tool result 做 HTML 流式输出。

### 7.3 StreamProcessingService 分发层

Cherry Studio 的 `StreamProcessingService` 会根据 chunk 类型调用不同回调，例如：

- `onTextStart`
- `onTextChunk`
- `onTextComplete`
- `onToolCallPending`
- `onToolCallInProgress`
- `onToolCallComplete`
- `onToolArgumentStreaming`
- `onImageCreated`
- `onImageDelta`
- `onImageGenerated`

整体流程可以表示为：

```txt
Provider / AI SDK stream
  -> Chunk
    -> StreamProcessingService
      -> callbacks
        -> message block update
          -> React UI rerender
```

### 7.4 工具调用状态处理层

Cherry Studio 有专门的工具调用 chunk handler，用于处理：

- 工具输入开始
- 参数流式接收
- 活跃工具调用记录
- 根据工具名判断 MCP tool、builtin tool 或 provider tool
- 生成 MCP tool 状态 chunk

所以模型在流式生成工具调用参数时，Cherry 已经可以把工具状态显示出来。

但这部分主要是工具调用过程可视化，不是当前项目的 HTML 结果流式输出。

### 7.5 Message block 状态管理层

Cherry Studio 的消息不是单一字符串，而是由多个 message block 组成，例如：

- text block
- tool block
- image block
- citation block
- code block

工具执行过程通常是：

```txt
创建 tool block
  -> pending / streaming
  -> in progress
  -> complete / error
  -> 更新 rawMcpToolResponse
```

这也是为什么在 Cherry Studio 中工具调用会作为独立卡片或块显示。

---

## 8. Cherry Studio 为什么能直接渲染 HTML

关键在于 Cherry Studio 的 Markdown 渲染器。

Cherry Studio 使用了：

```ts
import ReactMarkdown from 'react-markdown'
import rehypeRaw from 'rehype-raw'
```

并通过 `ReactMarkdown` 的 `rehypePlugins` 启用 raw HTML 解析。

`rehype-raw` 的作用是允许 Markdown 内容里的原始 HTML 被解析进 HTML AST，再转成 React 元素。

因此，当最终消息内容是：

```html
<div style="...">
  <h1>标题</h1>
  <p>正文</p>
</div>
```

Cherry Studio 不会把它当纯文本显示，而是会作为 raw HTML 解析并渲染。

这正是本项目能够在 Cherry Studio 中显示复杂 HTML 卡片和页面片段的核心原因。

---

## 9. “HTML 流式渲染”的真实发生位置

这里需要区分三种“流”。

### 9.1 LLM 文本流

这是最接近用户感知中“HTML 流式渲染”的部分。

流程是：

```txt
模型最终回答开始输出 HTML
  -> Cherry 收到 TEXT_DELTA
  -> 更新 text message block
  -> Markdown renderer 重新渲染
  -> rehypeRaw 解析当前已有 HTML
  -> UI 逐步更新
```

用户看到的是 HTML 内容一点点出现在消息里。但本质上，它是 HTML 字符串作为 assistant text stream 被流式输出。

### 9.2 MCP 工具调用流

Cherry Studio 也有 MCP 工具相关状态流：

```txt
MCP_TOOL_PENDING
MCP_TOOL_IN_PROGRESS
MCP_TOOL_STREAMING
MCP_TOOL_COMPLETE
```

但这些主要表示工具调用生命周期，尤其是参数生成和工具状态变化。当前项目的 MCP tool result 仍然是一次性返回。

### 9.3 本项目本地预览 SSE 流

项目内的 `src/live/previewServer.ts` 是真正的服务端事件流：

```txt
EventSource
  -> event: refresh
  -> preview page reload
```

但它用于本地预览，不是当前 Cherry Studio 消息区 HTML 渲染主链路。

---

## 10. 当前方案的优点

当前 one-shot HTML 渲染路线是合理的，原因如下。

### 10.1 避免 HTML 被工具卡片打断

如果模型在生成过程中多次调用 MCP 工具，Cherry Studio 会在消息中插入 tool block。这样会破坏最终 HTML 的连续性。

当前项目明确要求：

```txt
先完成搜索、阅读、分析和内容整理
最后只调用一次 MCP 工具
最终回复直接输出完整连续 HTML
```

这可以显著减少页面被工具卡片或普通文本打断的问题。

### 10.2 避免半截 HTML 导致布局不稳定

聊天消息区在流式渲染时，如果标签尚未闭合，可能出现：

- DOM 临时结构错误
- 样式抖动
- 列表或表格中途错乱
- raw HTML 暂时无法正确解析

一次性生成完整 HTML fragment 更稳定。

### 10.3 更容易控制安全边界

所有用户输入先进入结构化 schema，再由 renderer 统一转义和拼装，安全边界更清晰。

### 10.4 更适合长期维护

项目不是依靠大段 prompt 让模型自由写 HTML，而是通过：

```txt
schema
  -> contentTypes
  -> tokens
  -> blocks
  -> expressions
  -> renderer
```

来控制页面结构和视觉输出，可维护性更好。

---

## 11. 如果要做真正的流式 HTML，可选路线

如果后续希望从“视觉上的流式显示”升级为“系统层真正流式”，可以考虑三种路线。

### 11.1 路线 A：继续利用 Cherry 文本流

这是当前最推荐、兼容性最好的路线。

流程：

```txt
MCP 一次性返回完整 HTML
  -> 模型最终回答直接输出 HTML
  -> Cherry 通过 TEXT_DELTA 流式显示
```

优点：

- 不需要修改 Cherry Studio
- 不需要扩展 MCP 协议
- 与当前项目设计一致
- 兼容性最好

缺点：

- MCP 结果本身不是流式
- 大 HTML 可能需要模型再次输出，有 token 成本

### 11.2 路线 B：恢复或扩展本项目 live preview SSE

可以基于已有的：

```txt
src/live/previewServer.ts
src/live/liveStore.ts
```

设计一组高级工具，例如：

```txt
start_live_preview
append_live_section
update_live_section
replace_live_section
finalize_live_preview
```

工具返回本地预览 URL，Cherry Studio 消息区展示链接，真实页面在浏览器预览中通过 SSE 刷新。

优点：

- 可以实现真正的服务端 session 级实时更新
- 适合复杂页面预览和调试
- 可以复用现有 live preview 基础设施

缺点：

- Cherry 消息区内不是直接嵌入完整页面
- 需要重新暴露工具
- 会增加模型调用复杂度
- 可能破坏当前 one-shot 简洁性

### 11.3 路线 C：改造 Cherry Studio artifact-style HTML block

如果要实现类似 Artifact 或 Canvas 的能力，需要 Cherry Studio 客户端支持新的 HTML block 类型，例如：

```txt
MCP tool result streaming
  -> HTML artifact block
  -> incremental DOM update
  -> sandbox / iframe / shadow DOM
```

这已经不是单纯 MCP Server 能解决的问题，需要修改 Cherry Studio 客户端。

优点：

- 用户体验最好
- 可以支持真正的 HTML artifact
- 可以隔离样式和脚本风险

缺点：

- 需要 Cherry Studio 侧改造
- 安全模型更复杂
- 维护成本更高

---

## 12. 推荐后续演进方向

结合当前项目目标，建议继续坚持以下主线：

```txt
结构化输入
  -> Zod 校验
  -> 内容类型策略
  -> 自适应表达系统
  -> inline-styled HTML fragment
  -> one-shot output
```

优先演进方向如下。

### 12.1 强化自适应主题工具

`render_adaptive_theme_html` 是最值得继续投入的方向，可以继续扩展：

- 更多 expression type
- 更精细的 style profile
- 更稳定的 contentType 到表达策略映射
- 针对新闻、研究、对比、教程等场景的专用表达结构

### 12.2 增加高质量表达组件

建议优先完善：

- `evidence-map`
- `decision-matrix`
- `ranked-list`
- `process-guide`
- `timeline`
- `source-list`
- `section-outline`

这些组件比单纯视觉模板更有价值，因为它们直接提升信息表达质量。

### 12.3 加强 Cherry Studio 兼容测试

建议针对 Cherry 消息区实际行为做兼容测试：

- inline style 是否保留
- `<style>` 是否被屏蔽
- `<script>` 是否不可用
- 表格渲染是否稳定
- 链接是否安全
- 图片是否能正常展示
- 长 HTML 在流式输出时是否会出现中途解析问题

### 12.4 保持 one-shot 默认策略

不建议默认暴露 append、stream、partial render 类工具。它们容易导致：

- 工具卡片打断页面
- HTML 片段不连续
- 模型过早调用渲染工具
- 用户看到半成品页面

如果需要流式或实时预览，建议作为高级实验能力单独启用，而不是替代默认路径。

---

## 13. 小问题与改进点

当前项目存在一个小版本不一致问题：

- `package.json` 版本是 `0.3.1`
- `src/server/createMcpServer.ts` 中 MCP server version 是 `0.3.0`

建议后续顺手同步，避免 Cherry Studio 或日志中显示版本不一致。

---

## 14. 最终总结

当前项目的技术路线是清晰且合理的：

```txt
MCP stdio server
  -> 最终渲染工具
  -> 结构化 schema
  -> 受控 renderer
  -> inline-styled HTML fragment
  -> Cherry Studio raw HTML message rendering
```

Cherry Studio 能显示 HTML 并产生“流式渲染”效果，核心不是因为 MCP Server 在流式推送 HTML，而是因为 Cherry Studio 自身具备：

```txt
chunk-based streaming
  -> message block update
  -> ReactMarkdown
  -> rehypeRaw
  -> raw HTML 渲染
```

因此，当前项目应继续把重点放在“最终 HTML 生成质量”和“结构化表达能力”上，而不是过早追求 MCP 层面的 HTML 流式输出。对于 Cherry Studio 场景，最稳定的实践仍然是：

```txt
先完成全部搜索、阅读、分析和内容整理；
最后只调用一次 html-render-mcp；
最终回复只输出工具返回的完整连续 HTML 片段；
不要使用 Markdown 代码块包裹；
不要一段一段渲染。
```
