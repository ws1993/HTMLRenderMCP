# HTML Render MCP

面向 Cherry Studio 的本地 MCP Server，用于把完整结构化页面一次性渲染成可直接显示在对话消息里的 HTML 片段。

当前版本暴露两个最终渲染工具：

- `render_final_html`：最终一次性 HTML 渲染。模型应先完成全部搜索、阅读、分析和内容整理，最后调用一次本工具生成完整连续的 HTML 页面片段。
- `render_upgraded_html`：升级版最终一次性 HTML 渲染。它是完全独立的新工具，参考 `升级技术方案.md` 的“规则系统 + 设计令牌 + 内容类型策略 + 布局组件语法”方案，适合生成更结构化的研究、对比、教程、解读等页面。

这个项目不再默认暴露逐段渲染、外部预览、导出文件、校验等工具，避免模型在生成中途分段调用工具，导致 HTML 页面被工具卡片、思考内容或普通文本打断。

## 安装与构建

```bash
npm install
npm run build
```

开发调试：

```bash
npm run dev
```

正式运行：

```bash
npm start
```

## Cherry Studio MCP 配置

先在本目录执行：

```bash
npm install
npm run build
```

然后在 Cherry Studio 的 MCP 配置中添加：

```json
{
  "mcpServers": {
    "html-render-mcp": {
      "command": "node",
      "args": ["E:/Project/AI/HTMLRenderMCP/dist/index.js"],
      "cwd": "E:/Project/AI/HTMLRenderMCP"
    }
  }
}
```

如果你的 Cherry Studio 配置界面是表单形式，可以填写：

- 名称：`html-render-mcp`
- 命令：`node`
- 参数：`E:/Project/AI/HTMLRenderMCP/dist/index.js`
- 工作目录：`E:/Project/AI/HTMLRenderMCP`

## 推荐提示词

默认稳定渲染模式，调用 `render_final_html`：

```text
请先完成全部搜索、阅读、分析和内容整理，不要在搜索过程中调用 html-render-mcp。
等内容完整后，调用 html-render-mcp 的 render_final_html 一次性生成完整 HTML 页面片段。
最终回复输出工具返回的 HTML，不要使用 Markdown 代码块包裹 HTML，不要一段一段渲染。
```

升级技术方案渲染模式，调用 `render_upgraded_html`：

```text
请先完成全部搜索、阅读、分析和内容整理，不要在搜索过程中调用 html-render-mcp。
如果内容适合用“内容类型策略 + 布局组件 + 设计令牌”组织，请最后只调用一次 html-render-mcp 的 render_upgraded_html。
调用时按内容选择 contentTypes，例如 news、research、explain、compare、tutorial、list、opinion，并用 blocks 组织 hero、summary-card、timeline、stat-grid、comparison-table、quote、risk-box、faq、steps、source-list、callout 等区块。
最终回复只输出工具返回的完整连续 HTML 片段，不要使用 Markdown 代码块包裹 HTML，不要一段一段渲染。
```

## 工具输入

调用 `render_final_html`：

```json
{
  "page": {
    "template": "article",
    "title": "今日重要新闻",
    "description": "2026年5月27日 - 综合国内外热点",
    "theme": "modern-blue",
    "sections": [
      {
        "type": "hero",
        "heading": "今日重要新闻",
        "subheading": "2026年5月27日 - 国内外重大事件一览",
        "cta": {
          "label": "阅读全文",
          "href": "#content"
        }
      },
      {
        "type": "features",
        "heading": "国内重点",
        "intro": "今日国内重要新闻摘要",
        "items": [
          {
            "title": "山西煤矿事故",
            "body": "事故救援与安全生产排查成为关注重点。"
          },
          {
            "title": "海上执法行动",
            "body": "相关海域执法行动受到持续关注。"
          }
        ]
      },
      {
        "type": "content",
        "heading": "国际观察",
        "body": "整理完整资料后，模型只需要把结构化结果交给 MCP 做最终渲染。"
      }
    ],
    "footer": {
      "text": "Generated by HTML Render MCP"
    }
  }
}
```

如果客户端只能传字符串，`page` 也可以是完整页面对象的 JSON 字符串，服务端会先解析再校验。

调用 `render_upgraded_html`：

```json
{
  "page": {
    "title": "升级技术方案示例",
    "description": "基于内容类型策略和布局组件语法生成的完整 HTML 片段",
    "theme": "modern-blue",
    "contentTypes": ["research", "compare"],
    "tokens": {
      "primaryColor": "#2563eb",
      "accentColor": "#f59e0b",
      "fontScale": "normal",
      "cardRadius": "large",
      "spacingScale": "normal",
      "density": "normal",
      "borderStyle": "soft",
      "shadowLevel": "soft"
    },
    "blocks": [
      {
        "type": "hero",
        "eyebrow": "升级版渲染",
        "title": "规则系统 + 设计令牌，而不是模板库",
        "subtitle": "先完成信息整理，再用有限布局组件一次性输出完整 HTML。",
        "meta": ["research", "compare"],
        "highlights": [
          {
            "label": "内容类型",
            "value": "7 类",
            "detail": "覆盖新闻、研究、解读、对比、教程、清单和观点。"
          },
          {
            "label": "布局组件",
            "value": "11 种",
            "detail": "用有限组件组合稳定页面结构。"
          }
        ]
      },
      {
        "type": "summary-card",
        "title": "核心结论",
        "body": "不要让模型自由拼接复杂页面，而是让模型选择内容类型、填写结构化区块，再由 MCP 负责最终 HTML 渲染。",
        "items": [
          {
            "title": "规则稳定",
            "body": "页面结构来自固定组件语法，减少随机布局。"
          },
          {
            "title": "样式可控",
            "body": "视觉差异通过 design tokens 控制。"
          }
        ]
      },
      {
        "type": "comparison-table",
        "title": "模板库与规则系统对比",
        "columns": ["模板库", "规则系统"],
        "rows": [
          {
            "label": "维护成本",
            "cells": ["需要维护大量模板", "维护少量规则和组件"]
          },
          {
            "label": "生成稳定性",
            "cells": ["容易被模板数量拖累", "结构更统一，可预测性更高"]
          }
        ]
      },
      {
        "type": "source-list",
        "title": "参考来源",
        "items": [
          {
            "label": "升级技术方案.md",
            "description": "本工具的内容类型、布局组件和设计令牌参考来源。"
          }
        ]
      }
    ],
    "footer": {
      "text": "Generated by HTML Render MCP upgraded renderer"
    }
  }
}
```

## 支持的结构

模板：

- `landing-page`
- `report`
- `article`
- `dashboard`

主题：

- `modern-blue`
- `minimal-gray`
- `warm-orange`
- `dark-tech`

区块：

- `hero`
- `features`
- `content`
- `steps`
- `faq`

升级版内容类型（`render_upgraded_html`）：

- `news`
- `research`
- `explain`
- `compare`
- `tutorial`
- `list`
- `opinion`

升级版布局区块（`render_upgraded_html`）：

- `hero`
- `summary-card`
- `timeline`
- `stat-grid`
- `comparison-table`
- `quote`
- `risk-box`
- `faq`
- `steps`
- `source-list`
- `callout`

升级版设计令牌（`render_upgraded_html`）：

- `primaryColor`：主色，安全十六进制颜色，例如 `#2563eb`
- `accentColor`：强调色，安全十六进制颜色，例如 `#f59e0b`
- `fontScale`：`compact`、`normal`、`large`
- `cardRadius`：`none`、`small`、`medium`、`large`、`pill`
- `spacingScale`：`compact`、`normal`、`relaxed`
- `density`：`compact`、`normal`、`comfortable`
- `borderStyle`：`none`、`solid`、`soft`、`accent`
- `shadowLevel`：`none`、`soft`、`medium`、`strong`

## 输出约束

- 输出是片段级 HTML，不包含 `<!doctype>`、`<html>`、`<head>` 或 `<body>`，适合 Cherry Studio 消息区直接渲染。
- 样式全部使用行内样式，避免聊天消息环境屏蔽 `<style>`。
- 所有用户文本默认都会 HTML 转义；正文、说明、FAQ 答案等长文本只会放行少量安全内联标签：`<br>`、`<strong>`/`<b>`、`<em>`/`<i>`、`<u>`、`<s>`、`<code>`、`<mark>`、`<small>`、`<sub>`、`<sup>`。带属性的标签和其他标签仍会被转义。
- `render_final_html` 与 `render_upgraded_html` 都只负责最终 HTML 渲染，不负责搜索、写文件或启动外部预览。
- `render_upgraded_html` 是独立新增工具，不会改变 `render_final_html` 的输入结构和渲染行为。
