# Cherry Studio 渲染路线与 HTMLRenderMCP 适配总结

更新时间：2026-06-02

## 1. 核心结论

Cherry Studio 能同时支持 Markdown 和 HTML 渲染，本质上不是依赖某一个“神奇渲染器”，而是由以下几层共同完成：

```text
模型输出 / 用户内容
  -> Markdown / HTML 解析
  -> AST / HTML / React 组件树
  -> React Renderer
  -> Electron 内置 Chromium DOM 显示
```

当前 `HTMLRenderMCP` 之所以适配 Cherry Studio 较好，是因为它顺着 Cherry Studio 最稳定的消息渲染通道工作：

```text
模型调用 MCP 工具
  -> MCP 返回 text 类型的 HTML fragment 字符串
  -> 模型最终回复原样输出该 HTML fragment
  -> Cherry Studio 将其作为普通助手消息内容处理
  -> Cherry Studio 的 Markdown/HTML 渲染链路把它显示成可视页面
```

换句话说，`HTMLRenderMCP` 没有试图接管 Cherry Studio 的 UI，也没有要求外部 WebView、文件预览或脚本执行，而是生成 Cherry Studio 消息区最容易消费的形态：**一次性、连续、内联样式、无脚本、结构完整的 HTML 片段**。

## 2. Cherry Studio 为什么能渲染 Markdown

Cherry Studio 是基于 Electron、React、Vite、TypeScript 的桌面客户端。它的聊天内容、笔记、富文本编辑等场景天然需要处理 Markdown。源码和依赖显示它具备完整的 Markdown 处理栈。

关键依赖包括：

- `react-markdown`
- `markdown-it`
- `@shikijs/markdown-it`
- `remark-parse`
- `remark-stringify`
- `unified`
- `rehype-katex`
- `rehype-mathjax`
- `mermaid`
- `katex`

Markdown 内容通常会经过如下路径：

```text
Markdown 文本
  -> markdown-it / unified / remark 解析
  -> Markdown AST 或 HTML 字符串
  -> 自定义 React 组件或 DOM 节点
  -> Electron Chromium 渲染
```

在 Cherry Studio 源码中可以看到它并不是简单地把 Markdown 文本直接塞进页面，而是会对常见 Markdown 节点做定制处理：

- 标题、段落、列表、引用、表格等 block 节点会被解析并渲染成结构化 DOM。
- code block 会进行 HTML entity escape、语言标识处理、语法高亮或代码块增强。
- 表格有专门的复制、导出、Markdown 源码提取逻辑。
- LaTeX 公式会被预处理，再交给 KaTeX / MathJax 生态渲染。
- Mermaid / Graphviz 等图形内容有独立预览或增强渲染路径。

因此 Cherry Studio 的 Markdown 渲染路线更准确地说是：

```text
Markdown -> AST/HTML -> 自定义组件 -> React -> Chromium DOM
```

## 3. Cherry Studio 为什么能渲染 HTML

Cherry Studio 支持 HTML 的原因主要有三点。

### 3.1 Markdown 解析器允许 raw HTML 或具备 raw HTML 处理能力

Cherry Studio 的依赖中有：

- `rehype-raw`
- `rehype-parse`
- `htmlparser2`
- `html-tags`
- `html-to-text`
- `turndown`
- `dompurify`
- `markdown-it`

其中：

- `rehype-raw` 用于把 Markdown 中的原生 HTML 继续解析进 HTML AST。
- `markdown-it({ html: true })` 可以允许 Markdown 中的 HTML 标签进入最终渲染结果。
- `turndown` 用于 HTML 转 Markdown。
- `html-to-text` 用于从 HTML 中提取文本。
- `dompurify` 用于清理潜在危险 HTML。

这说明 Cherry Studio 的内容系统不是“只懂 Markdown”，而是具备 Markdown 与 HTML 互相嵌入、互相转换的能力。

### 3.2 RichEditor / Tiptap 内部本来就以 HTML/DOM 为核心表示之一

Cherry Studio 的 RichEditor 使用 Tiptap / ProseMirror。Tiptap 是基于文档模型和 DOM 的富文本编辑器，内部可以天然维护 HTML 表示。

源码路径中可以看到类似逻辑：

```text
editor.getText()
editor.getHTML()
htmlToMarkdown(htmlContent)
onHtmlChange(htmlContent)
onMarkdownChange(convertedMarkdown)
```

这说明 Cherry Studio 的编辑和显示体系中，HTML 是一等格式之一，并不是 Markdown 的附属品。

### 3.3 Electron 的 Chromium 渲染引擎天然支持 HTML/CSS

Cherry Studio 运行在 Electron 中，最终显示层是 Chromium。只要内容经过安全策略允许并进入 DOM，常规 HTML/CSS 都可以渲染。

因此，Cherry Studio 对 HTML 的支持可以概括为：

```text
HTML 字符串 / Markdown 中的 HTML
  -> rehype / markdown-it / Tiptap / DOM parser
  -> 安全过滤与组件化处理
  -> React DOM / Chromium DOM
  -> 可视化显示
```

需要注意的是，Cherry Studio 支持的是“被其渲染管线和安全策略接受的 HTML 片段”，不是无约束执行的完整网页环境。危险内容通常会被过滤或限制，例如：

- `<script>`
- `onload` / `onclick` 等事件属性
- `javascript:` 链接
- 未受控的 iframe
- 某些 data URI 或 base64 图片场景

## 4. 为什么当前 HTMLRenderMCP 适配 Cherry Studio 很好

### 4.1 协议层适配：Cherry Studio 原生支持 MCP

Cherry Studio 官方文档提供了 MCP 使用说明，并支持在 `Settings - MCP Server` 中配置 MCP 服务。

当前项目使用 `@modelcontextprotocol/sdk` 实现标准 MCP Server，并在 `src/server/createMcpServer.ts` 中声明：

```ts
capabilities: {
  tools: {}
}
```

同时处理：

- `ListToolsRequestSchema`
- `CallToolRequestSchema`

因此它符合 Cherry Studio 调用 MCP 工具的基本协议要求。

### 4.2 输出层适配：返回 MCP text content

当前项目的 `src/server/toolResponse.ts` 会把工具结果包装成：

```ts
content: [
  {
    type: "text",
    text: html
  }
]
```

这对 Cherry Studio 很关键。因为 Cherry Studio 最稳定、最常见的渲染入口就是助手消息文本。只要模型最终回复时直接输出这段 HTML，而不是放进 Markdown 代码块，Cherry Studio 的 Markdown/HTML 渲染链路就有机会将其作为 HTML 片段渲染。

### 4.3 形态层适配：输出 HTML fragment，而不是完整 HTML document

当前 MCP 的 inline renderer 输出类似：

```html
<div data-html-render-mcp="inline" style="...">
  ...
</div>
```

而不是：

```html
<!doctype html>
<html>
  <head>...</head>
  <body>...</body>
</html>
```

这非常适合聊天消息区。因为聊天消息通常不是完整浏览器页面，`<head>`、`<style>`、`script>`、完整 document 结构都可能被忽略、过滤或破坏。HTML fragment 更适合嵌入现有消息 DOM。

### 4.4 样式层适配：使用 inline style，减少对外部 CSS 的依赖

当前项目的 inline renderer 会把视觉样式写入 `style` 属性，例如：

```html
<div style="margin:16px 0; background:...; border:...">
```

这种方式对 Cherry Studio 友好，原因是：

- 不依赖外部 CSS 文件。
- 不依赖 Cherry Studio 内部 class 名。
- 不需要注入 `<style>` 标签。
- 不容易被 Markdown 容器截断。
- 在普通消息 DOM 中更容易保持视觉一致。

### 4.5 安全层适配：MCP 输出的是低风险 HTML

当前项目没有生成脚本，也没有要求动态运行时。源码中有多处安全处理：

- `escapeHtml.ts` 会转义 `&`、`<`、`>`、`"`、`'`。
- `escapeAttribute` 会进一步处理属性上下文。
- `normalizeRenderableHref.ts` 只允许以下协议：
  - `http:`
  - `https:`
  - `mailto:`
  - `tel:`

这让 MCP 输出的 HTML 更容易通过 Cherry Studio 的安全策略，也降低了消息渲染中的 XSS 风险。

### 4.6 生成策略适配：一次性渲染，避免流式半成品破坏 HTML

AI 对话是流式输出。如果模型一边思考一边分段输出 HTML，很容易出现：

- 标签未闭合
- 表格结构未完成
- Markdown code fence 未闭合
- HTML 被普通文本或工具卡片打断
- 多段 HTML 之间样式和结构不连续

当前 `HTMLRenderMCP` 明确采用 one-shot 策略：

```text
先搜索、阅读、分析、整理内容
  -> 最后只调用一次 MCP 工具
  -> MCP 返回完整连续 HTML 片段
  -> 最终回复只输出该 HTML
```

这个策略与 Cherry Studio 的消息渲染方式高度匹配。

### 4.7 结构层适配：模型填结构化数据，MCP 负责稳定生成 HTML

当前项目暴露四个最终渲染工具：

- `render_final_html`
- `render_upgraded_html`
- `render_adaptive_theme_html`
- `render_information_structure_html`

这些工具都通过 Zod schema 校验输入，再由 renderer 统一生成 HTML。好处是：

- 模型不用直接手写复杂 HTML。
- 页面结构更稳定。
- 样式更统一。
- 错误更容易定位。
- 能避免未闭合标签、危险链接、布局随机漂移等问题。

## 5. 当前项目的推荐渲染路线

推荐在 Cherry Studio 中使用如下路线：

```text
1. 模型先完成全部搜索、阅读、分析、内容整理。
2. 根据内容类型选择 MCP 工具：
   - 普通页面：render_final_html
   - 研究、对比、教程、解读等结构化页面：render_upgraded_html
   - 需要主题自适应的信息表达：render_adaptive_theme_html
   - 需要新闻 / 研究 / 科普 / 对比 / 教程 / 清单 / 观点骨架：render_information_structure_html
3. 最后只调用一次工具。
4. 工具返回完整连续 HTML fragment。
5. 最终回复直接输出该 HTML fragment。
6. 不要使用 Markdown 代码块包裹 HTML。
7. 不要分段调用或分段渲染。
```

推荐提示词：

```text
请先完成全部搜索、阅读、分析和内容整理，不要在搜索过程中调用 html-render-mcp。
等内容完整后，最后只调用一次 html-render-mcp 的最终渲染工具，生成完整连续的 HTML 页面片段。
最终回复只输出工具返回的 HTML，不要使用 Markdown 代码块包裹 HTML，不要一段一段渲染。
```

## 6. 边界与注意事项

虽然当前 MCP 适配 Cherry Studio 较好，但仍需注意以下边界：

1. **不要生成脚本型页面**  
   Cherry Studio 消息渲染不应依赖 `<script>`、外部 JS、事件属性或复杂运行时。

2. **不要依赖完整 document 结构**  
   聊天消息适合 HTML fragment，不适合完整 `<!doctype html><html>...</html>` 页面。

3. **尽量使用 inline style**  
   外部 CSS、全局 class、`<style>` 标签都可能受到客户端策略影响。

4. **链接必须走安全协议**  
   当前 MCP 已限制为 `http:`、`https:`、`mailto:`、`tel:`，这应继续保留。

5. **避免超长单段 HTML**  
   页面可以复杂，但结构应清晰，避免单个节点过深或单段文本过长导致客户端渲染卡顿。

6. **最终输出不要包裹代码块**  
   如果写成 ```html 代码块，Cherry Studio 会把它当代码展示，而不是当 HTML 渲染。

## 7. 参考来源

- Cherry Studio GitHub 仓库：<https://github.com/CherryHQ/cherry-studio>
- Cherry Studio MCP 文档：<https://docs.cherry-ai.com/docs/en-us/advanced-basic/mcp/install>
- Cherry Studio `package.json`：<https://raw.githubusercontent.com/CherryHQ/cherry-studio/101904d03518e601ad2970242b2f29b15219073b/package.json>
- Cherry Studio `markdownConverter.ts`：<https://raw.githubusercontent.com/CherryHQ/cherry-studio/101904d03518e601ad2970242b2f29b15219073b/src/renderer/src/utils/markdownConverter.ts>
- Cherry Studio `markdown.ts`：<https://raw.githubusercontent.com/CherryHQ/cherry-studio/101904d03518e601ad2970242b2f29b15219073b/src/renderer/src/utils/markdown.ts>
- 当前项目 README：`README.md`
- 当前项目 MCP Server：`src/server/createMcpServer.ts`
- 当前项目工具注册：`src/server/toolRegistry.ts`
- 当前项目响应包装：`src/server/toolResponse.ts`
- 当前项目 inline renderer：`src/renderers/renderInlineHtml.ts`
- 当前项目安全转义：`src/utils/escapeHtml.ts`
- 当前项目链接过滤：`src/utils/normalizeRenderableHref.ts`

## 8. 最终判断

Cherry Studio 的优势在于它已经具备完整的 Markdown/HTML 消息渲染基础设施；`HTMLRenderMCP` 的优势在于它输出的正是 Cherry Studio 消息系统最容易接受的 HTML 片段。

因此，二者的最佳协作方式不是“让 MCP 创建外部网页”，而是：

```text
MCP 负责结构化生成安全稳定的 HTML fragment；
Cherry Studio 负责把该 fragment 作为消息内容渲染出来。
```

这也是当前 MCP 能很好适配 Cherry Studio 的根本原因。
