 # Cherry Studio 连续对话实现方法总结

 ## 核心结论

 Cherry Studio 的连续对话不是依赖模型服务端保存会话状态，也不是依赖 provider-side 的 `session_id` 或 `conversation_id`。它采用的是典型的 **无状态 API + 客户端历史消息重放** 方案：每一轮请求前，客户端从当前 Topic 中取出历史消息，经过上下文过滤、裁剪和格式转换后，把历史消息与当前用户问题一起发送给模型。

 简化来说：

 ```text
 当前 Topic 历史消息
   -> 截取到当前 user message 为止
   -> 过滤 clear / 空消息 / 错误消息 / 多模型重复回复
   -> 根据 contextCount 裁剪
   -> 转成 AI SDK / OpenAI 类 messages
   -> 连同 system prompt 一起请求模型
 ```

 ## 整体调用链路

 一轮普通聊天的大致流程如下：

 ```text
 用户输入
   -> getUserMessage 创建 user Message 和 MessageBlock
   -> sendMessage thunk 保存用户消息
   -> 创建空的 assistant message
   -> assistant message 的 parentId / askId 指向 user message
   -> 放入 Topic 队列
   -> fetchAndProcessAssistantResponseImpl 构造上下文
   -> ConversationService.filterMessagesPipeline 过滤上下文
   -> convertMessagesToSdkMessages 转换为模型输入
   -> ApiService.fetchChatCompletion 调用模型
   -> 流式返回写入 StreamingService / BlockManager
   -> 最终持久化 assistant message 和 blocks
 ```

 关键源码文件：

 - `src/renderer/store/thunk/messageThunk.ts`
 - `src/renderer/services/ConversationService.ts`
 - `src/renderer/utils/messageUtils/filters.ts`
 - `src/renderer/services/ApiService.ts`
 - `src/renderer/aiCore/prepareParams/messageConverter.ts`
 - `src/renderer/aiCore/prepareParams/parameterBuilder.ts`
 - `src/renderer/services/messageStreaming/StreamingService.ts`
 - `src/renderer/types/newMessage.ts`

 ## 消息数据结构

 Cherry Studio 使用 `Topic + Message + MessageBlock` 表示会话。

 ### Message

 `Message` 的关键字段包括：

 ```ts
 type Message = {
   id: string
   role: 'user' | 'assistant' | 'system'
   assistantId: string
   topicId: string
   status: UserMessageStatus | AssistantMessageStatus

   modelId?: string
   model?: Model
   type?: 'clear'
   useful?: boolean
   askId?: string
   mentions?: Model[]

   blocks: MessageBlock['id'][]
 }
 ```

 重要字段含义：

 - `topicId`：消息所属会话主题。
 - `role`：消息角色，包含 user / assistant / system。
 - `askId`：assistant 消息关联的 user message ID。
 - `type: 'clear'`：新上下文分隔标记。
 - `useful`：多模型回复中标记哪条回复进入后续上下文。
 - `blocks`：消息正文、图片、文件、工具调用、错误、引用等内容块。

 ### MessageBlock

 常见 block 类型包括：

 - `MAIN_TEXT`：主要文本。
 - `THINKING`：模型推理内容。
 - `IMAGE`：图片。
 - `FILE`：文件。
 - `TOOL`：工具调用。
 - `CITATION`：引用。
 - `ERROR`：错误。
 - `COMPACT`：压缩上下文结果。

 这使 Cherry Studio 的上下文不只是简单文本拼接，还能处理多模态和工具结果。

 ## 发送消息阶段

 在 `messageThunk.ts` 的 `sendMessage` 中，Cherry Studio 会先保存用户消息，再创建一个待生成的 assistant message。

 普通 Topic 中，用户消息通过：

 ```ts
 streamingService.createUserMessage(topicId, userMessage, userMessageBlocks)
 ```

 创建 assistant message 时会传入：

 ```ts
 streamingService.createAssistantMessage(topicId, {
   parentId: finalUserMessage.id,
   assistantId: assistant.id,
   modelId: assistant.model?.id,
   model: assistant.model,
   siblingsGroupId: 0,
   traceId: finalUserMessage.traceId ?? undefined
 })
 ```

 其中 `parentId` 指向当前 user message。`StreamingService` 会把它映射成 renderer 旧格式中的 `askId`：

 ```ts
 askId: parentId
 ```

 因此，assistant 回复和 user 提问之间的关系是通过 `parentId / askId` 建立的。

 ## 上下文构造方法

 真正构造连续对话上下文的位置在 `fetchAndProcessAssistantResponseImpl`。

 它会先取出当前 Topic 的全部消息：

 ```ts
 const allMessagesForTopic = selectMessagesForTopic(getState(), topicId)
 ```

 然后找到当前 assistant message 对应的 user message：

 ```ts
 const userMessageId = assistantMessage.askId
 const userMessageIndex = allMessagesForTopic.findIndex((m) => m?.id === userMessageId)
 ```

 接着只截取到当前 user message 为止：

 ```ts
 const contextSlice = allMessagesForTopic.slice(0, userMessageIndex + 1)
 messagesForContext = contextSlice.filter((m) => m && !m.status?.includes('ing'))
 ```

 这个设计保证：

 - 当前空的 assistant stub 不会进入上下文。
 - 当前 user 之后的消息不会进入上下文。
 - 正在生成中的消息不会污染上下文。
 - 如果找不到当前 user，会有 fallback 避免上下文为空。

 最后将 `messagesForContext` 传给：

 ```ts
 transformMessagesAndFetch({ messages: messagesForContext, assistant, ... })
 ```

 ## 上下文过滤管线

 核心过滤逻辑在 `ConversationService.filterMessagesPipeline`：

 ```ts
 static filterMessagesPipeline(messages: Message[], contextCount: number): Message[] {
   const messagesAfterContextClear = filterAfterContextClearMessages(messages)
   const usefulMessages = filterUsefulMessages(messagesAfterContextClear)
   const withoutErrorOnlyPairs = filterErrorOnlyMessagesWithRelated(usefulMessages)
   const withoutTrailingAssistant = filterLastAssistantMessage(withoutErrorOnlyPairs)
   const withoutAdjacentUsers = filterAdjacentUserMessaegs(withoutTrailingAssistant)
   const limitedByContext = takeRight(withoutAdjacentUsers, contextCount + 2)
   const contextClearFiltered = filterAfterContextClearMessages(limitedByContext)
   const nonEmptyMessages = filterEmptyMessages(contextClearFiltered)
   const userRoleStartMessages = filterUserRoleStartMessages(nonEmptyMessages)
   return userRoleStartMessages
 }
 ```

 ### 过滤规则说明

 1. **只保留最后一次 clear 之后的消息**

    `filterAfterContextClearMessages` 会找到最后一条 `type === 'clear'` 的消息，只保留它后面的历史。

 2. **多模型回复只选一条进入上下文**

    `filterUsefulMessages` 会按 assistant 消息组筛选：

    - 如果有 `useful === true` 的回复，保留该回复。
    - 如果没有 useful 标记，默认保留该组第一条 assistant 回复。

    GitHub PR #8833 也说明了这一点：消息组中没有“有用”消息时，默认选择第一条加入上下文。

 3. **移除只有错误的 assistant 回复及其关联 user**

    `filterErrorOnlyMessagesWithRelated` 会移除 error-only assistant 消息，并移除对应的 user 消息，避免错误轮次污染上下文。

 4. **移除尾部 assistant 消息**

    `filterLastAssistantMessage` 会移除上下文末尾连续的 assistant 消息。

 5. **相邻 user 消息只保留最后一条**

    `filterAdjacentUserMessaegs` 会在连续 user 消息中保留最后一条。

 6. **按 contextCount 裁剪**

    使用：

    ```ts
    takeRight(withoutAdjacentUsers, contextCount + 2)
    ```

    `contextCount` 来自助手设置，默认值为：

    ```ts
    DEFAULT_CONTEXTCOUNT = 5
    ```

    `MAX_CONTEXT_COUNT = 100` 会被内部转换为 `UNLIMITED_CONTEXT_COUNT = 100000`。

 7. **移除空消息，并保证上下文从 user 开始**

    `filterEmptyMessages` 和 `filterUserRoleStartMessages` 会保证最终送给模型的消息有效且角色顺序合理。

 ## 转换为模型输入

 过滤后的 UI 消息会进入：

 ```ts
 ConversationService.prepareMessagesForModel(messages, assistant)
 ```

 其中核心逻辑是：

 ```ts
 const uiMessagesFromPipeline = ConversationService.filterMessagesPipeline(messages, contextCount)

 return {
   modelMessages: await convertMessagesToSdkMessages(uiMessages, assistant.model || getDefaultModel()),
   uiMessages
 }
 ```

 `convertMessagesToSdkMessages` 位于：

 ```text
 src/renderer/aiCore/prepareParams/messageConverter.ts
 ```

 它会把 Cherry Studio 自己的 `Message + MessageBlock` 转成 AI SDK 的 `ModelMessage[]`。

 用户消息一般会转成：

 ```ts
 {
   role: 'user',
   content: parts
 }
 ```

 assistant 消息一般会转成：

 ```ts
 {
   role: 'assistant',
   content: parts
 }
 ```

 `parts` 可以包含文本、图片、文件、推理内容等。

 对视觉模型，Cherry Studio 还有特殊逻辑：会把上一轮 assistant 里的图片合并进最后一条 user 消息，用于图像编辑或图像续聊。

 ## 最终模型请求

 模型请求在 `ApiService.fetchChatCompletion` 中执行。

 调用链为：

 ```text
 transformMessagesAndFetch
   -> ConversationService.prepareMessagesForModel
   -> fetchChatCompletion
   -> buildStreamTextParams
   -> AI.completions
 ```

 `buildStreamTextParams` 会构造 AI SDK 参数：

 ```ts
 const params: StreamTextParams = {
   messages: sdkMessages,
   maxOutputTokens,
   temperature,
   topP,
   abortSignal,
   headers,
   providerOptions,
   maxRetries: 0
 }
 ```

 assistant prompt 会作为 system prompt：

 ```ts
 params.system = systemPrompt
 ```

 如果当前 Topic 自己也有 prompt，Cherry Studio 会把它拼接到 assistant prompt 后：

 ```ts
 prompt: `${origAssistant.prompt}\n${topic.prompt}`
 ```

 最终给模型的请求结构可以理解为：

 ```ts
 {
   system: assistant.prompt + topic.prompt,
   messages: [
     历史 user,
     历史 assistant,
     ...,
     当前 user
   ],
   tools,
   providerOptions,
   temperature,
   topP,
   maxOutputTokens
 }
 ```

 ## 流式回复与持久化

 Cherry Studio 使用 `StreamingService` 管理流式生成过程。

 生成开始时调用：

 ```ts
 streamingService.startTask(topicId, assistantMsgId, {
   parentId: userMessageId,
   contextMessages: messagesForContext,
   ...
 })
 ```

 流式任务会暂存在内存缓存中，典型 key 包括：

 ```text
 message.streaming.task.${messageId}
 message.streaming.topic_tasks.${topicId}
 message.streaming.content.${messageId}
 message.streaming.block.${blockId}
 ```

 生成过程中：

 - `BlockManager` 创建和更新消息块。
 - Redux 临时更新 UI。
 - 流式结束后 `finalize` 持久化到 Data API 或旧数据库。
 - 最后清理 streaming cache。

 这让 Cherry Studio 可以边生成边显示，同时把完整回复保存下来，供下一轮连续对话使用。

 ## 多模型回复场景

 Cherry Studio 支持一个 user message 同时触发多个模型回复。

 实现方式：

 - 多个 assistant message 拥有相同 `parentId / askId`。
 - 多模型回复共享同一个 `siblingsGroupId`。
 - UI 使用 `getGroupedMessages` 把它们分组展示。
 - 上下文使用 `filterUsefulMessages` 确保只有一条 assistant 回复进入下一轮上下文。

 上下文选择策略：

 ```text
 如果某条 assistant 回复 useful === true，则选择它；
 否则默认选择该组第一条 assistant 回复。
 ```

 ## 新上下文机制

 Cherry Studio 的“新上下文”不是简单删除历史，而是在消息历史中插入一条 `type: 'clear'` 的 user message。

 后续构造上下文时，`filterAfterContextClearMessages` 会只保留最后一个 clear 标记之后的消息。

 因此，新上下文的本质是：

 ```text
 保留历史记录，但后续请求模型时从 clear 标记之后开始取上下文。
 ```

 ## 简化伪代码

 ```ts
 async function sendMessage(userInput) {
   const userMessage = createUserMessage(userInput)
   const savedUser = await saveUserMessage(userMessage)

   const assistantMessage = await createAssistantMessage({
     parentId: savedUser.id
   })

   queue.add(() => generateAssistantResponse(assistantMessage))
 }

 async function generateAssistantResponse(assistantMessage) {
   const allMessages = selectMessagesForTopic(topicId)

   const currentUserIndex = allMessages.findIndex(
     m => m.id === assistantMessage.askId
   )

   let messagesForContext = allMessages
     .slice(0, currentUserIndex + 1)
     .filter(m => !m.status?.includes('ing'))

   const uiMessages = filterMessagesPipeline(
     messagesForContext,
     assistant.settings.contextCount
   )

   const modelMessages = await convertMessagesToSdkMessages(
     uiMessages,
     assistant.model
   )

   await AI.completions({
     system: assistant.prompt + topic.prompt,
     messages: modelMessages
   })
 }
 ```

 ## 实现特点

 ### 优点

 - 实现简单可靠，不依赖模型服务端会话状态。
 - 兼容不同模型和 provider。
 - 支持多模型回复，并通过 `useful` 控制哪条进入上下文。
 - 支持新上下文，不必删除历史消息。
 - 通过 MessageBlock 支持文本、图片、文件、工具调用、引用和推理内容。
 - 流式生成和最终持久化解耦，用户体验较好。

 ### 局限

 - 普通聊天每轮都要重放历史消息，provider 侧并不保存会话。
 - 上下文主要按消息条数裁剪，不是严格按 token 动态裁剪。
 - 多模型回复如果用户不手动标记 useful，默认选择第一条，不一定总是最佳回复。
 - 源码中消息系统正处于 v2 数据/UI 重构阶段，存在 deprecated/TODO 注释。

 ## 一句话总结

 Cherry Studio 的连续对话实现方式是：**以 Topic 为会话单位保存完整消息历史，每轮请求前基于当前 user message 截取历史，经过 clear、useful、error、空消息、contextCount 等过滤规则后，转换成模型 messages，再随 system prompt 一起发送给模型。**

